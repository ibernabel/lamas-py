# Project Conventions — Software Domain

> This file is auto-generated by `project-init` for every software project.
> Copy it to `.agents/CONVENTIONS.md` and fill in the project-specific values.
> The agent reads this file at the start of every task to enforce project standards.

---

## Project Identity

```yaml
project:
  name: "LAMaS"
  domain: software
  vertical: "technology"
  pii: financial
  primary_stack: [fastapi, sqlmodel, pydantic, postgresql, nextjs, typescript, tailwind]
  secondary_stack: [docker, pytest, vitest, nextauth]
```

---

## Git Conventions

```yaml
git:
  branch_convention: "feature/[domain]-[short-description]"
  # Examples:
  #   feature/admin-client-onboarding
  #   feature/auth-jwt-refresh
  #   feature/api-credit-calculator
  #   fix/api-null-pointer-on-empty-response

  protected_branches:
    - main
    - develop

  no_direct_push_to: main
  # The agent MUST refuse to write production code without an active branch.
  # Verify with: git branch --show-current

  commit_convention: conventional-commits
  # Prefixes: feat, fix, docs, chore, refactor, test, style, perf, ci, build
  # Scope (optional): area of the codebase affected, e.g. feat(auth): add JWT refresh
```

---

## Pipeline Rules

```yaml
pipeline:
  # Changes that MUST go through the full Uncle Bob pipeline:
  trigger_on:
    - feature    # new functionality
    - epic       # major module or domain addition

  # Changes that bypass the pipeline (direct mode, still require a branch):
  direct_mode_on:
    - fix        # bug fixes
    - tweak      # small adjustments (CSS, copy, config)
    - docs       # documentation only
    - chore      # dependencies, scripts, CI

  # Pipeline order (never skip steps):
  # orchestrator → specifier → coder → refactorer → architect → qa → [pii-verifier if financial]
```

---

## Coverage & Testing Standards

```yaml
coverage:
  strategy: gherkin-scenarios
  # Definition of Done: 100% of Gherkin scenarios defined by the Specifier MUST have passing tests.
  # Line coverage % is NOT the gate — scenario coverage is.

  minimum_line_coverage: null      # not enforced
  mutation_testing: true           # Architect runs mutation testing (CRAP ≤ 6)
  e2e_required: false              # Set to true if the project has a UI and Playwright is configured
```

---

## DDD Conventions (if applicable)

```yaml
ddd:
  enabled: true
  bounded_contexts: [customer-management, loan-applications, document-management, creditgraph-ai, settings-i18n]
  ubiquitous_language:
    Customer: "Borrower or client entity in the system"
    LoanApplication: "Request for microloan subject to credit assessment"
    CreditGraph: "AI scoring and credit risk evaluation engine"
    NID: "Dominican National ID Card (Cédula: 000-0000000-0)"
    SoliPres: "Legacy system name and visual identity pattern"
```

---

## PII & Security

```yaml
security:
  pii_scan_on_merge: false         # Overridden to true when pii = financial (see above)
  secrets_in_env: true             # All secrets MUST be in .env, never hardcoded
  env_example_required: true       # .env.example MUST exist and be committed
  input_sanitization: required     # All user inputs must be validated (Zod, Pydantic, etc.)
```

---

## Notes

<!-- Add any project-specific notes, known issues, or exceptions to the rules above -->
